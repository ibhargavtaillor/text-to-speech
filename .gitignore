# Dependencies
node_modules/
# pnpm-lock.yaml is committed for reproducible installs
package-lock.json
yarn.lock

# Build / bundle output
dist/
build/
.vite/
*.zip          # packaged extension uploads

# Logs & caches
*.log
npm-debug.log*
.cache/
.eslintcache

# Environment / secrets (e.g. cloud TTS API keys for future export feature)
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Claude local config (keep out of version control)
.claude/
