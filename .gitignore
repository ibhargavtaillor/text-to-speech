# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build / bundle output (if you add esbuild/rollup later)
dist/
build/
*.zip          # packaged extension uploads

# Logs & caches
*.log
npm-debug.log*
.cache/
.eslintcache

# Environment / secrets (e.g. cloud TTS API keys for future export feature)
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Claude local config (keep out of version control)
.claude/
